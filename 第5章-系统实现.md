# 第5章 贡江社区二手商品交易平台的实现

本章按"主界面 → 账号 → 商品 → 交易 → 消息 → 邻里互助 → 后台管理 → AI 智能"的顺序，对各模块的实现进行说明。

**统一约定**：

- 后端基于 Spring Boot 2.x + MyBatis + JWT + Hutool MD5，每个 Controller 返回统一封装的 `Result<T>`（`{ code, msg, data }`）。
- 前端基于 Vue 3 Composition API + Element Plus + Pinia + Axios，登录态通过 Pinia 持久化到 `localStorage`，请求统一在 axios 拦截器中携带 `Authorization` 头。
- 图片上传走 `/upload` 接口，返回相对 URL 后写入对应表的 `images / avatar` 字段。
- 全部截图以"图 5-N"形式编号，并在正文中引用。

---

## 5.1 系统主界面

### 5.1.1 用户端首页

**功能描述**　用户登录后的默认落地页，提供顶部搜索栏、轮播图、分类导航、最新商品瀑布流，并在右下角悬浮 AI 客服入口。

**实现思路**　`Home.vue` 在 `onMounted` 钩子中并行请求 `/banner/list`、`/category/list`、`/goods/list?status=1` 三个接口，分别填充顶部 Banner、分类菜单与商品列表；商品卡片点击跳转商品详情，分类点击跳转高级搜索页并携带 `categoryId`。

**关键代码**：

```javascript
onMounted(async () => {
  const [banners, cats, list] = await Promise.all([
    api.get('/banner/list'),
    api.get('/category/list'),
    api.get('/goods/list', { params: { status: 1 } })
  ])
  bannerList.value   = banners.data
  categoryList.value = cats.data
  goodsList.value    = list.data
})
```

如图 5-1 所示，页面自上而下依次为搜索栏、轮播图、分类入口、商品瀑布流。

![图 5-1 用户端首页](images/5-1-home.png)

### 5.1.2 后台管理首页

**功能描述**　管理员登录后首先进入 Dashboard，展示用户数、在售商品数、订单数、待审核商品数、待仲裁退款数等关键指标。

**实现思路**　`admin/Dashboard.vue` 聚合 `/user/list`、`/goods/list?status=0`、`/order/list`、`/refund/admin/arbitrating` 等接口的返回长度生成指标卡，并以 ECharts 柱状图展示近 7 日新增订单。

如图 5-2 所示，Dashboard 顶部为 5 张数据卡片，下方为走势图与最新动态列表。

![图 5-2 后台管理首页](images/5-2-dashboard.png)

---

## 5.2 用户账号模块

### 5.2.1 手机号验证码注册

**功能描述**　新用户输入手机号、用户名、密码后点击"获取验证码"，5 分钟内输入收到的 6 位数字完成注册。验证码发送频率限制：60 秒/条、单手机号每日最多 10 条。

**实现思路**　前端 `Register.vue` 调用 `POST /sms/send`（参数 `purpose=register`）触发 `SmsService.sendCode`，写入 `sms_code` 表；提交注册时后端 `UserService.register` 校验：① 用户名/手机号唯一；② 验证码未使用且未过期；③ MD5 加密后插入 `user` 表。沙箱模式下验证码直接打印到后端日志。

**关键代码**（后端注册接口）：

```java
@PostMapping("/register")
public Result<?> register(@RequestBody Map<String, Object> body) {
    User user = new User();
    user.setUsername((String) body.get("username"));
    user.setPassword((String) body.get("password"));
    user.setPhone((String) body.get("phone"));
    String code = (String) body.get("code");
    return Result.success(userService.register(user, code));
}
```

如图 5-3 所示，注册页右侧为表单，验证码按钮在 60 秒倒计时期间为禁用状态。

![图 5-3 用户注册](images/5-3-register.png)

### 5.2.2 用户登录

**功能描述**　支持"用户名 + 密码"登录，登录成功后服务端签发 JWT，前端写入 Pinia store 并持久化到 `localStorage`，过期或失效时由拦截器跳转登录页。

**实现思路**　`UserService.login` 校验用户状态（`status=0` 表示封禁，拒绝登录），密码经 Hutool `SecureUtil.md5` 加密后比对；通过则签发 JWT 并返回 `{ token, user }`。

**关键代码**（前端 axios 拦截器）：

```javascript
http.interceptors.request.use(cfg => {
  const token = useUserStore().token
  if (token) cfg.headers.Authorization = `Bearer ${token}`
  return cfg
})
```

如图 5-4 所示，登录页左侧为品牌标语，右侧为登录表单，底部提供注册和找回密码的入口。

![图 5-4 用户登录](images/5-4-login.png)

### 5.2.3 找回密码

**功能描述**　用户输入手机号、获取验证码、设置新密码即可完成密码重置。复用 5.2.1 的短信通道，但验证码 `purpose=reset`。

**实现思路**　`ResetPassword.vue` 提交后调用 `POST /user/reset-password`，后端校验验证码后用新密码 MD5 覆盖 `user.password`。

如图 5-5 所示，找回密码页与注册页布局一致以减少认知成本。

![图 5-5 找回密码](images/5-5-reset.png)

### 5.2.4 个人信息管理

**功能描述**　用户可在"我的"页面修改昵称、邮箱、头像；卖家可在"卖家主页设置"中维护店铺简介与联系方式。

**实现思路**　头像由 `UploadController` 接收并保存至本地 `uploads/` 目录，返回 URL 后由 `PUT /user/info` 写入 `user.avatar`；密码修改单独走 `PUT /user/password`，要求传入 `oldPassword` 校验。

如图 5-6 所示，"我的"页面采用左侧侧栏 + 右侧表单布局，可一屏内完成所有信息编辑。

![图 5-6 个人信息管理](images/5-6-my.png)

### 5.2.5 收货地址管理

**功能描述**　用户可维护多条收货地址，支持设为默认。下单时从该列表中选择。

**实现思路**　`AddressManage.vue` 提供新增 / 编辑 / 删除 / 设默认四个动作，对应 `address` 表 CRUD；设为默认时 Service 端将该用户其余记录的 `is_default` 重置为 0。

如图 5-7 所示，地址卡片右上角为操作按钮，默认地址带有"默认"标签。

![图 5-7 收货地址管理](images/5-7-address.png)

---

## 5.3 商品模块

### 5.3.1 商品发布

**功能描述**　用户填写标题、描述、价格、新旧程度、分类，并上传 1–9 张商品图后提交，商品状态为"待审核"，待管理员通过后才可在前台展示。

**实现思路**　`Publish.vue` 通过 `<el-upload>` 多次调用 `/upload` 拿到图片 URL 数组并 JSON 序列化；表单提交至 `POST /goods/publish`，后端 `GoodsService.publish` 默认置 `status=0`（待审核）。本页同时集成 5.8.1 的 AI 模板生成：用户填好标题后可一键生成详情描述。

**关键代码**：

```java
@PostMapping("/publish")
public Result<?> publish(@RequestBody Goods goods) {
    return Result.success(goodsService.publish(goods));
}
```

如图 5-8 所示，发布页左侧为表单字段，右侧"AI 帮我写"按钮可调用模板生成。

![图 5-8 商品发布](images/5-8-publish.png)

### 5.3.2 商品浏览与分类

**功能描述**　首页瀑布流和分类页均使用同一商品列表组件，仅 `categoryId` 参数不同。

**实现思路**　`GET /goods/list` 接收 `keyword / categoryId / status / minPrice / maxPrice / conditions / timeRange / sort` 八个可选参数，由 `GoodsMapper` 用动态 SQL 拼接 WHERE 子句，未传参数则不参与过滤。该接口同时服务于首页、分类页与高级搜索页。

复用了图 5-1 的瀑布流容器，按分类筛选后效果不再单独配图。

### 5.3.3 商品搜索与高级筛选

**功能描述**　用户可按关键词、分类、价格区间、新旧程度（多选）、发布时间（3天/7天/30天）筛选，并可按"最新发布 / 价格升 / 价格降 / 浏览量降"排序。

**实现思路**　`AdvancedSearch.vue` 收集筛选条件后拼接为 query string 跳转刷新；后端在 `GoodsController.list` 中将 `conditions=1,2` 拆为 `List<Integer>`，将 `timeRange=7d` 解析为 7 天前的 `Date`，再交给 Mapper 处理。

**关键代码**（后端参数解析）：

```java
private Date parseSinceTime(String timeRange) {
    if (timeRange == null) return null;
    int days; switch (timeRange) {
        case "3d":  days = 3;  break;
        case "7d":  days = 7;  break;
        case "30d": days = 30; break;
        default: return null;
    }
    Calendar c = Calendar.getInstance();
    c.add(Calendar.DAY_OF_MONTH, -days);
    return c.getTime();
}
```

如图 5-9 所示，左侧为筛选面板，右侧为结果列表，顶部显示当前条件标签。

![图 5-9 商品高级搜索](images/5-9-search.png)

### 5.3.4 商品详情查看

**功能描述**　展示商品大图、标题、价格、新旧程度、卖家信息、商品评价；提供"立即购买""收藏""联系卖家""举报"操作。每次访问 `view_count` 自增。

**实现思路**　`GoodsDetail.vue` 拉取 `/goods/{id}` 与 `/review/goods/{goodsId}`；底部"联系卖家"跳转私信页面并带入卖家 ID；进入页面时后端在 Service 层执行 `viewCount + 1`。

如图 5-10 所示，左侧为图片轮播，右侧为商品信息与操作按钮，下方为评价列表。

![图 5-10 商品详情](images/5-10-goods-detail.png)

### 5.3.5 商品收藏

**功能描述**　用户在商品详情页或卡片上点击爱心图标可收藏；"我的收藏"页可查看收藏列表，点击进入详情或取消收藏。

**实现思路**　`favorite` 表 `(user_id, goods_id)` 联合唯一约束防止重复收藏。`Favorites.vue` 拉取 `/favorite/list/{userId}` 仅返回 goodsId 列表，再并行请求每个商品详情拼装为完整商品卡，确保删除商品时收藏自动失效。

如图 5-11 所示，"我的收藏"页采用与首页相同的卡片样式。

![图 5-11 我的收藏](images/5-11-favorites.png)

### 5.3.6 卖家主页

**功能描述**　点击商品卡片或详情页上的卖家昵称，跳转到该卖家主页，展示其卖家简介、信用评分（来自 5.4.7 评价聚合）、在售商品列表与历史评价。

**实现思路**　`SellerHome.vue` 同时拉取 `/user/{id}`、`/goods/my/{userId}`、`/review/seller/{sellerId}/stats`、`/review/seller/{sellerId}` 四个接口，前端组合渲染。卖家自己访问时显示"编辑主页"按钮跳到 `SellerProfile.vue`。

如图 5-12 所示，主页顶部为店铺标头，下方分"在售商品 / 历史评价"两个标签页。

![图 5-12 卖家主页](images/5-12-seller-home.png)

---

## 5.4 交易模块

### 5.4.1 下单确认

**功能描述**　从商品详情进入下单页，用户选择"邮寄到家"或"社区自提"，填写收货地址或自提点，可填备注，确认后生成订单并跳转支付页。

**实现思路**　`OrderConfirm.vue` 中 `deliveryType=1`（邮寄）显示地址列表，`deliveryType=2`（自提）显示自提点下拉与"约定见面时间"选择器；提交时调用 `POST /order/create`，后端生成 `order_no`（时间戳 + 随机后缀）、置 `status=0`、设 `expire_time = now + 30min`，并把对应商品 `status` 改为 2（已售出）防止并发下单。

**关键代码**（前端配送方式切换）：

```vue
<button :class="{ active: deliveryType === 1 }" @click="deliveryType = 1">
  📦 邮寄到家
</button>
<button :class="{ active: deliveryType === 2 }" @click="deliveryType = 2">
  🏡 社区自提
</button>
```

如图 5-13 所示，页面分为商品信息、配送方式、收货/自提点、订单备注、提交按钮 5 个卡片。

![图 5-13 下单确认](images/5-13-order-confirm.png)

### 5.4.2 在线支付（沙箱模式）

**功能描述**　订单创建后跳转支付页，展示二维码占位、订单金额、剩余支付时间。沙箱模式下提供"模拟支付成功"按钮，点击后订单进入"待发货"。

**实现思路**　前端 `Pay.vue` 调用 `POST /payment/create` 创建支付流水（返回 `qrPayload`、`gatewayMode`），并以 3 秒间隔轮询 `GET /payment/status/{orderId}` 监听状态变化；沙箱按钮调用 `POST /payment/sandbox/notify` 触发支付成功回调，把 `order.status` 改为 1、`payment.status` 改为 1，并写入 `pay_time`。该层抽出 `PaymentGateway` 接口，留有支付宝/微信真实接入位。

**关键代码**：

```java
@PostMapping("/sandbox/notify")
public Result<?> sandboxNotify(@RequestBody Map<String, String> body) {
    return Result.success(paymentService.sandboxNotify(body.get("orderNo")));
}
```

如图 5-14 所示，左侧为生成的二维码，右侧为订单信息与"模拟支付"按钮（仅沙箱模式下显示）。

![图 5-14 在线支付](images/5-14-pay.png)

### 5.4.3 订单查看与状态流转

**功能描述**　"我的订单"页支持按"全部 / 待付款 / 待发货 / 待收货 / 已完成 / 已关闭"标签页过滤，并区分"我买的"与"我卖的"两个视角。

**实现思路**　买家视角调用 `GET /order/buy/{buyerId}`，卖家视角调用 `GET /order/sell/{sellerId}`；卖家在"待发货"状态可点击"发货"调用 `PUT /order/ship/{id}`，状态流转为"待收货"。

如图 5-15 所示，订单卡片右下角的操作按钮按状态动态切换。

![图 5-15 我的订单](images/5-15-orders.png)

### 5.4.4 取消订单 / 超时关闭

**功能描述**　买家可主动取消"待付款"状态的订单；超过 30 分钟未支付的订单由后台定时任务自动关闭。

**实现思路**　主动取消调用 `PUT /order/cancel/{id}`，仅允许 `status=0` 的订单。超时关闭由 `@Scheduled` 任务每分钟扫描 `status=0 AND expire_time < now()` 的订单批量改为 `status=4`，并把对应商品 `status` 还原为 1（重新上架）。

### 5.4.5 确认收货

**功能描述**　买家在"待收货"状态点击"确认收货"，订单流转为"已完成"，触发评价入口和（在沙箱中）T+1 结算计时。

**实现思路**　`PUT /order/receive/{id}` 校验当前状态为 2，置 `status=3`、写入 `complete_time`；同时把商品 `status` 设为 2（已售出最终态），并向买卖双方推送评价提醒。

### 5.4.6 售后退款与仲裁

**功能描述**　订单完成后 7 日内买家可发起退款，提交原因和凭证图片；卖家在 48 小时内同意或拒绝；超时后买家可申请平台仲裁，由管理员裁决。

**实现思路**　`refund` 表 `status` 字段共 7 种取值：0 待卖家处理 / 1 卖家同意 / 2 卖家拒绝 / 3 申请仲裁 / 4 仲裁同意 / 5 仲裁拒绝 / 6 已退款。`RefundController` 提供 `apply`、`seller/agree`、`seller/reject`、`arbitrate`、`admin/agree`、`admin/reject` 共 6 个状态转换接口；定时任务每小时扫描 `apply_time + 48h < now()` 且仍是 `status=0` 的记录，自动开放仲裁入口。

**关键代码**：

```java
@PutMapping("/seller/agree/{id}")
public Result<?> sellerAgree(@PathVariable Integer id, @RequestParam Integer sellerId) {
    return Result.success(refundService.sellerAgree(id, sellerId));
}
@PutMapping("/arbitrate/{id}")
public Result<?> arbitrate(@PathVariable Integer id, @RequestParam Integer buyerId) {
    return Result.success(refundService.applyArbitrate(id, buyerId));
}
```

如图 5-16 所示，退款详情页用步骤条直观呈现"申请→卖家处理→仲裁→退款完成"全流程。

![图 5-16 退款详情](images/5-16-refund.png)

### 5.4.7 交易评价

**功能描述**　订单完成后买卖双方互评，评分维度按角色不同：买家评卖家（描述相符 / 沟通态度 / 发货速度），卖家评买家（付款速度 / 沟通态度 / 信用度），各维度 1–5 星，可附文字与图片。15 天未评价由系统默认好评。

**实现思路**　`review` 表通过 `UNIQUE(order_id, role)` 保证每方仅一次。`ReviewService.submit` 以三维平均分写入 `overall_score`；定时任务扫描 `complete_time + 15d < now()` 且无对应评价的订单，插入 `auto_default=1` 的默认好评。

**关键代码**：

```java
@PostMapping("/submit")
public Result<?> submit(@RequestBody Map<String, Object> body) {
    Integer orderId    = toInt(body.get("orderId"));
    Integer fromUserId = toInt(body.get("fromUserId"));
    Integer role       = toInt(body.get("role"));
    int item1 = toInt(body.get("item1"));
    int item2 = toInt(body.get("item2"));
    int item3 = toInt(body.get("item3"));
    return Result.success(reviewService.submit(
            orderId, fromUserId, role, item1, item2, item3,
            (String) body.get("content"), body.get("images").toString()));
}
```

如图 5-17 所示，提交评价页采用 3 行星级控件 + 文本框 + 图片上传。

![图 5-17 交易评价](images/5-17-review.png)

---

## 5.5 消息模块

### 5.5.1 私信交流

**功能描述**　买家在商品详情点击"联系卖家"进入私信会话，支持文本消息；消息可携带商品上下文（点击跳回商品）。

**实现思路**　`POST /message/send` 写入 `message` 表（`from_id / to_id / goods_id / content / is_read=0`）；进入会话窗口时调用 `GET /message/conversation?userId1=A&userId2=B` 拉取双向历史。

### 5.5.2 消息列表展示

**功能描述**　"消息"页左侧为会话列表，分"私信"与"系统通知"两个 Tab；每条会话显示对方头像、最近一条消息预览、未读数。

**实现思路**　`GET /message/inbox/{userId}` 在 SQL 中按 `MAX(create_time)` 聚合每个会话最新一条；未读数由同次查询统计。点击会话调用 `PUT /message/read?fromId=&toId=` 批量标记为已读。

如图 5-18 所示，会话列表项 hover 高亮，未读会话显示红色徽标。

![图 5-18 消息列表](images/5-18-messages.png)

### 5.5.3 消息实时刷新

**功能描述**　会话窗口在打开期间每 3 秒拉取一次最新消息，未打开的会话在右上角铃铛显示总未读数。

**实现思路**　采用前端轮询而非 WebSocket，简化部署。`Messages.vue` 中通过 `setInterval` 每 3 秒调用 `inbox` 接口；进入具体会话时切换为对该会话的 `conversation` 轮询。同时 `User.heartbeat` 接口被定时调用以维护 `user.last_active_time`，作为"在线"标识依据。

```javascript
const timer = setInterval(loadInbox, 3000)
onUnmounted(() => clearInterval(timer))
```

---

## 5.6 邻里互助模块

### 5.6.1 互助帖发布

**功能描述**　用户可发布求购、赠送、换物三种类型的互助帖，填写标题、描述、图片、所在位置、可选联系方式。

**实现思路**　`CommunityPublish.vue` 提交至 `POST /post/publish`，写入 `community_post` 表，`type` 字段为 1/2/3 区分类型。换物类型额外开启"想换的物品"输入框（对应字段 `want_item`）。

如图 5-19 所示，发布页根据 `type` 单选切换不同的字段集。

![图 5-19 互助帖发布](images/5-19-community-publish.png)

### 5.6.2 帖子浏览与回复

**功能描述**　帖子列表按类型 Tab 切换，支持关键词搜索；详情页显示发帖人信息、正文、回复列表，登录用户可发帖回复。

**实现思路**　`GET /post/list?type=&keyword=` 由 `CommunityPostService.search` 动态拼接条件；详情页 `GET /post/{id}` 同时通过 `GET /post/replies/{postId}` 拉取回复列表，发表回复 `POST /post/reply` 后局部刷新并 `reply_count + 1`。

如图 5-20 所示，详情页正文区下方为时间倒序的回复列表。

![图 5-20 互助帖详情](images/5-20-community-detail.png)

### 5.6.3 安全自提点查询

**功能描述**　下单页"社区自提"模式下展示自提点下拉，附名称、地址、开放时间、说明（如"物业旁、摄像头覆盖"）。

**实现思路**　`PickupPointController.list` 返回 `status=1` 的所有自提点；社区初始化脚本预置 4 个示例点（社区服务中心、1 号楼物业、东门快递柜、西门岗亭）。下单选中后保存 `pickup_point_id` 与 `meet_time` 到订单。

如图 5-21 所示，下拉项以"名称 + 开放时间"双行展示，便于快速识别。

![图 5-21 安全自提点](images/5-21-pickup.png)

---

## 5.7 后台管理模块

### 5.7.1 管理员登录

**功能描述**　独立的 `/admin/login` 页面，与用户登录隔离；管理员账号在数据库中通过 `admin` 表维护，超级管理员（`role=2`）可管理普通管理员（`role=1`）。

**实现思路**　`POST /admin/login` 同样使用 MD5 + JWT，但 token 中加入 `role` 声明；前端路由守卫对 `/admin/**` 路径校验 `role`，未登录或角色不符跳转管理员登录页。

如图 5-22 所示，管理员登录页采用深色品牌风格以与用户端区分。

![图 5-22 管理员登录](images/5-22-admin-login.png)

### 5.7.2 用户管理

**功能描述**　展示全部用户列表，支持按用户名/手机号搜索，可对违规用户执行封禁/解封操作（切换 `status` 0/1）。

**实现思路**　`GET /user/list` 一次性拉取（用户量有限），表格使用 `<el-table>` + 客户端过滤；封禁调用 `PUT /user/status/{id}/{status}` 后被封用户在下次登录时被 Service 层拒绝。

如图 5-23 所示，每行末尾的"封禁/解封"按钮根据当前状态切换文案与颜色。

![图 5-23 用户管理](images/5-23-admin-user.png)

### 5.7.3 商品审核

**功能描述**　审核新发布的商品，可"通过"或"驳回"。通过后商品 `status` 由 0 变为 1（在售），驳回则置为 3（下架）。

**实现思路**　`GET /goods/list?status=0` 拉取待审核列表；审核动作调用 `PUT /goods/status/{id}/{status}`。表格行内可展开查看商品图片大图。

如图 5-24 所示，待审核 Tab 用红色徽标提示数量。

![图 5-24 商品审核](images/5-24-admin-goods.png)

### 5.7.4 订单管理

**功能描述**　管理员可查看全部订单及其状态、买卖双方、金额、时间线，用于客服处理纠纷场景。

**实现思路**　`GET /order/list` 拉取全表，前端按 `status` 分页展示；管理员可强制改单状态走 `PUT /order/status/{id}/{status}`（通常用于异常订单回滚）。

### 5.7.5 退款仲裁

**功能描述**　处理买家发起的仲裁，管理员阅读买家原因、卖家回复、凭证图片后选择"同意退款"或"维持不退"，并填写裁决说明。

**实现思路**　`GET /refund/admin/arbitrating` 拉取仲裁池（`status=3` 的记录），裁决通过 `PUT /refund/admin/agree/{id}` 或 `PUT /refund/admin/reject/{id}` 提交；同意时 Service 端联动调用支付通道退款（沙箱直接置 `payment.status=3`），并把 `refund.status` 改为 6（已退款）。

如图 5-25 所示，仲裁页顶部为待办数量徽标，下方为详情卡片与裁决按钮。

![图 5-25 退款仲裁](images/5-25-admin-refund.png)

### 5.7.6 分类管理

**功能描述**　对 `category` 表进行 CRUD，可调整 `sort` 决定首页分类顺序，可禁用某分类阻止后续商品归类到它。

**实现思路**　全部走 `category` 表的标准 CRUD 接口，禁用使用软删 `status=0`，已发布商品不受影响。

### 5.7.7 公告管理

**功能描述**　管理员发布、编辑、上下线公告；用户端 `Notice.vue` 展示 `status=1` 的公告。

**实现思路**　发布时记录发布者 `admin_id`，便于审计追溯。

### 5.7.8 轮播图管理

**功能描述**　维护首页 Banner 的图片、跳转链接、排序、上下线状态。

**实现思路**　图片走 `/upload`，记录走 `banner` 表 CRUD；首页只读取 `status=1` 且按 `sort` 升序的记录。

### 5.7.9 自提点管理

**功能描述**　管理员可新增/编辑/停用自提点，下单页实时反映。

**实现思路**　`PickupPointController` 提供标准 CRUD，停用通过 `status=0` 实现，避免历史订单关联失效。

---

## 5.8 AI 智能模块

### 5.8.1 商品描述模板生成

**功能描述**　卖家在发布商品时输入标题，点击"AI 帮我写"按钮即可生成 100–200 字商品详情描述，可一键填入或继续编辑。

**实现思路**　`POST /ai/describe` 由 `AiService.generateDescription` 调用阿里云 DashScope 的 `qwen-turbo` 模型，使用预置 system prompt（强调风格简洁、突出新旧程度与亮点）；返回的纯文本写入 `Publish.vue` 表单的 description 字段。

**关键代码**：

```java
@PostMapping("/describe")
public Result<?> describe(@RequestBody Map<String, String> body) {
    String description = aiService.generateDescription(body.get("title"));
    Map<String, String> data = new HashMap<>();
    data.put("description", description);
    return Result.success(data);
}
```

如图 5-26 所示，弹窗显示生成结果与"使用此文案 / 重新生成"按钮。

![图 5-26 AI 模板生成](images/5-26-ai-template.png)

### 5.8.2 AI 客服小贡对话

**功能描述**　每个登录用户右下角悬浮气泡入口"小贡"，点击展开对话窗口，可咨询定价建议、交易流程、商品推荐等。窗口内保留多轮上下文。

**实现思路**　`AIChat.vue` 维护 `messages` 数组（含 user / ai 双向气泡），每次发送时把最近 N 条历史 + 当前消息打包为 `{ userId, message, history }` 发往 `POST /ai/chat`，由 `AiService.chat` 拼接成 DashScope 多轮 messages 格式调用 `qwen-turbo`。

**关键代码**（后端 chat 接口）：

```java
@PostMapping("/chat")
@SuppressWarnings("unchecked")
public Result<?> chat(@RequestBody Map<String, Object> body) {
    Integer userId = toInt(body.get("userId"));
    String message = String.valueOf(body.get("message"));
    List<Map<String, String>> history = (List<Map<String, String>>) body.getOrDefault("history",
            Collections.emptyList());
    String reply = aiService.chat(userId, history, message);
    Map<String, String> data = new HashMap<>();
    data.put("reply", reply);
    return Result.success(data);
}
```

如图 5-27 所示，会话窗口顶部为机器人头像，初次进入展示快捷场景按钮。

![图 5-27 AI 客服小贡](images/5-27-ai-chat.png)

---

## 5.9 本章小结

本章按"主界面 → 账号 → 商品 → 交易 → 消息 → 邻里互助 → 后台管理 → AI 智能"8 个模块，对贡江社区二手商品交易平台的实现进行了说明。系统覆盖了从注册、发布、浏览、下单、支付、收货、评价、退款全交易闭环，并围绕"社区"主题扩展了邻里互助板块与安全自提点机制；通过沙箱化的支付与短信通道，使整套流程可在无真实第三方依赖的开发环境中端到端运行。后台管理涵盖用户、商品、订单、退款仲裁、分类、公告、轮播图、自提点 8 项常规职能；AI 模块借助阿里云 DashScope 的 `qwen-turbo` 模型在商品发布与客服咨询两个高频场景中提升用户效率。下一章将围绕功能性测试、压力测试与安全性测试三个维度对系统进行验证。
