# 数据库表设计

数据库名：`ylw`（MySQL 8，字符集 utf8mb4）。下表列出系统当前在用的全部 18 张数据表，按业务模块顺序排列。

---

## 表 4-1 user 用户表

存储平台买卖双方的注册账号信息，包括登录凭据、个人资料和封禁状态，是所有业务的主体身份来源。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码（MD5 加密） |
| nickname | VARCHAR(50) |  | 昵称 |
| avatar | VARCHAR(255) |  | 头像 URL |
| phone | VARCHAR(20) | UNIQUE | 手机号（注册唯一） |
| email | VARCHAR(100) |  | 邮箱 |
| status | TINYINT | DEFAULT 1 | 1 正常 / 0 封禁 |
| last_active_time | DATETIME |  | 最后心跳时间 |
| create_time | DATETIME | DEFAULT NOW | 注册时间 |
| update_time | DATETIME | ON UPDATE | 更新时间 |

---

## 表 4-2 admin 管理员表

存储后台管理员账号，用于登录管理后台对用户、商品、订单、退款仲裁等进行管控。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码（MD5 加密） |
| role | TINYINT | DEFAULT 1 | 1 普通管理员 / 2 超级管理员 |
| create_time | DATETIME | DEFAULT NOW | 创建时间 |

---

## 表 4-3 category 商品分类表

维护一级商品分类目录（数码电子、服装鞋帽、书籍教材等），供商品发布时挂载和首页导航展示。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| name | VARCHAR(50) | NOT NULL | 分类名称 |
| icon | VARCHAR(255) |  | 分类图标 |
| sort | INT | DEFAULT 0 | 排序（越小越靠前） |
| status | TINYINT | DEFAULT 1 | 1 启用 / 0 禁用 |

---

## 表 4-4 goods 商品表

记录用户发布的二手商品信息，包括标题描述、价格、图片、新旧程度和上下架状态，是交易系统的核心实体。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| user_id | INT | NOT NULL, INDEX | 发布者 ID |
| category_id | INT | NOT NULL, INDEX | 分类 ID |
| title | VARCHAR(100) | NOT NULL | 商品标题 |
| description | TEXT |  | 商品描述 |
| price | DECIMAL(10,2) | NOT NULL | 售价 |
| original_price | DECIMAL(10,2) |  | 原价 |
| images | TEXT |  | 图片 URL 列表（JSON 数组） |
| condition_level | TINYINT |  | 1 全新 / 2 几乎全新 / 3 有使用痕迹 |
| status | TINYINT | DEFAULT 0, INDEX | 0 待审核 / 1 在售 / 2 已售出 / 3 下架 |
| view_count | INT | DEFAULT 0 | 浏览量 |
| create_time | DATETIME | DEFAULT NOW | 发布时间 |
| update_time | DATETIME | ON UPDATE | 更新时间 |

---

## 表 4-5 address 收货地址表

保存用户的多个收货地址，支持设置默认地址，下单时关联到 order 表。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| user_id | INT | NOT NULL, INDEX | 用户 ID |
| receiver | VARCHAR(50) | NOT NULL | 收货人姓名 |
| phone | VARCHAR(20) | NOT NULL | 联系电话 |
| detail | VARCHAR(255) | NOT NULL | 详细地址 |
| is_default | TINYINT | DEFAULT 0 | 1 默认 / 0 非默认 |

---

## 表 4-6 order 订单表

承载交易主流程的核心表，跟踪从下单、支付、发货、收货到结算关闭的完整生命周期，支持邮寄与社区自提两种交付方式。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_no | VARCHAR(50) | NOT NULL, UNIQUE | 订单号 |
| goods_id | INT | NOT NULL, INDEX | 商品 ID |
| buyer_id | INT | NOT NULL, INDEX | 买家 ID |
| seller_id | INT | NOT NULL, INDEX | 卖家 ID |
| price | DECIMAL(10,2) | NOT NULL | 成交价格 |
| address_id | INT |  | 收货地址 ID |
| delivery_type | TINYINT | DEFAULT 1 | 1 邮寄 / 2 自提 |
| pickup_point_id | INT |  | 自提点 ID |
| meet_time | DATETIME |  | 约定见面时间 |
| status | TINYINT | DEFAULT 0, INDEX | 0 待付款 / 1 待发货 / 2 待收货 / 3 已完成 / 4 已关闭 |
| remark | VARCHAR(255) |  | 买家备注 |
| pay_type | VARCHAR(20) |  | alipay / wechat / sandbox |
| expire_time | DATETIME |  | 支付超时时间 |
| pay_time | DATETIME |  | 支付成功时间 |
| ship_time | DATETIME |  | 发货时间 |
| complete_time | DATETIME |  | 确认收货时间 |
| close_time | DATETIME |  | 订单关闭时间 |
| settle_time | DATETIME |  | 平台结算时间（T+1 打款给卖家） |
| create_time | DATETIME | DEFAULT NOW | 下单时间 |
| update_time | DATETIME | ON UPDATE | 更新时间 |

---

## 表 4-7 payment 支付流水表

记录每笔支付请求的网关响应、二维码内容、第三方交易号和状态，与订单解耦以支持重复发起与对账。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_id | INT | NOT NULL, INDEX | 订单 ID |
| order_no | VARCHAR(50) | NOT NULL, INDEX | 订单号 |
| pay_type | VARCHAR(20) | NOT NULL | alipay / wechat / sandbox |
| amount | DECIMAL(10,2) | NOT NULL | 支付金额 |
| trade_no | VARCHAR(64) | INDEX | 第三方交易号 |
| qr_payload | VARCHAR(512) |  | 支付二维码内容 / 跳转链接 |
| status | TINYINT | DEFAULT 0 | 0 待支付 / 1 已支付 / 2 已关闭 / 3 已退款 |
| raw_response | TEXT |  | 网关原始响应 |
| create_time | DATETIME | DEFAULT NOW | 创建时间 |
| pay_time | DATETIME |  | 支付成功时间 |

---

## 表 4-8 favorite 收藏表

记录用户对商品的收藏关系，通过 user_id + goods_id 唯一约束防止重复收藏。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| user_id | INT | NOT NULL | 用户 ID |
| goods_id | INT | NOT NULL | 商品 ID |
| create_time | DATETIME | DEFAULT NOW | 收藏时间 |
| — | — | UNIQUE(user_id, goods_id) | 防重复收藏 |

---

## 表 4-9 comment 评论表

存储商品详情页的咨询/留言评论及评分，独立于交易完成后的评价（review 表）。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| goods_id | INT | NOT NULL, INDEX | 商品 ID |
| user_id | INT | NOT NULL | 评论用户 ID |
| content | VARCHAR(500) | NOT NULL | 评论内容 |
| rating | TINYINT |  | 评分 1–5 |
| create_time | DATETIME | DEFAULT NOW | 评论时间 |

---

## 表 4-10 message 私信表

承载买卖双方的一对一即时消息，可关联具体商品上下文，支持已读未读状态。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| from_id | INT | NOT NULL, INDEX | 发送者 ID |
| to_id | INT | NOT NULL, INDEX | 接收者 ID |
| goods_id | INT |  | 关联商品 ID |
| content | VARCHAR(500) | NOT NULL | 消息内容 |
| is_read | TINYINT | DEFAULT 0 | 1 已读 / 0 未读 |
| create_time | DATETIME | DEFAULT NOW | 发送时间 |

---

## 表 4-11 notice 公告表

存放管理员发布的平台公告内容，前台首页或站内消息处展示。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| title | VARCHAR(100) | NOT NULL | 公告标题 |
| content | TEXT | NOT NULL | 公告内容 |
| admin_id | INT |  | 发布管理员 ID |
| status | TINYINT | DEFAULT 1 | 1 发布 / 0 下线 |
| create_time | DATETIME | DEFAULT NOW | 发布时间 |

---

## 表 4-12 banner 轮播图表

管理首页轮播图素材，支持图片、跳转链接、排序与上下线状态。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| image | VARCHAR(255) | NOT NULL | 图片 URL |
| link | VARCHAR(255) |  | 点击跳转链接 |
| sort | INT | DEFAULT 0 | 排序 |
| status | TINYINT | DEFAULT 1 | 1 显示 / 0 隐藏 |

---

## 表 4-13 refund 退款/售后申请表

记录交易完成后买家发起的退款/售后流程，含卖家响应、48 小时超时机制与管理员仲裁裁决全过程。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_id | INT | NOT NULL, UNIQUE | 订单 ID（一单一申请） |
| order_no | VARCHAR(50) | NOT NULL | 订单号冗余 |
| buyer_id | INT | NOT NULL, INDEX | 买家 ID |
| seller_id | INT | NOT NULL, INDEX | 卖家 ID |
| amount | DECIMAL(10,2) | NOT NULL | 退款金额 |
| reason | VARCHAR(500) | NOT NULL | 退款原因 |
| images | TEXT |  | 凭证图片 JSON 数组 |
| status | TINYINT | DEFAULT 0, INDEX | 0 待卖家处理 / 1 卖家同意 / 2 卖家拒绝 / 3 申请仲裁 / 4 仲裁同意 / 5 仲裁拒绝 / 6 已退款 |
| seller_reply | VARCHAR(500) |  | 卖家拒绝原因 |
| admin_note | VARCHAR(500) |  | 管理员仲裁说明 |
| apply_time | DATETIME | DEFAULT NOW | 申请时间 |
| seller_respond_time | DATETIME |  | 卖家响应时间 |
| arbitrate_time | DATETIME |  | 申请仲裁时间 |
| admin_respond_time | DATETIME |  | 管理员裁决时间 |
| refund_time | DATETIME |  | 退款完成时间 |
| deadline | DATETIME | NOT NULL | 卖家响应截止（apply_time + 48h） |

---

## 表 4-14 review 交易评价表

订单完成后买卖双方互评的双向三维评分表，UNIQUE(order_id, role) 保证一单每方各只能评一次，支持 15 天超时默认好评。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_id | INT | NOT NULL | 订单 ID |
| order_no | VARCHAR(50) | NOT NULL | 订单号 |
| goods_id | INT | NOT NULL, INDEX | 商品 ID |
| from_user_id | INT | NOT NULL, INDEX | 评价人 |
| to_user_id | INT | NOT NULL, INDEX | 被评价人 |
| role | TINYINT | NOT NULL | 0 买家评卖家 / 1 卖家评买家 |
| item1_score | TINYINT | NOT NULL | 维度 1 评分 1–5 |
| item2_score | TINYINT | NOT NULL | 维度 2 评分 1–5 |
| item3_score | TINYINT | NOT NULL | 维度 3 评分 1–5 |
| overall_score | DECIMAL(3,2) | NOT NULL | 三维平均分 |
| content | VARCHAR(500) |  | 评价文本 |
| images | TEXT |  | 图片 URL JSON 数组 |
| auto_default | TINYINT | DEFAULT 0 | 0 用户评价 / 1 系统默认好评 |
| create_time | DATETIME | DEFAULT NOW | 评价时间 |
| — | — | UNIQUE(order_id, role) | 一单每方仅一条 |

---

## 表 4-15 sms_code 短信验证码表

存放注册和找回密码的手机短信验证码，限制有效期 5 分钟、60 秒重发间隔与日上限 10 条。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| phone | VARCHAR(20) | NOT NULL, INDEX | 手机号 |
| code | VARCHAR(10) | NOT NULL | 验证码 |
| purpose | VARCHAR(20) | NOT NULL | register 注册 / reset 重置密码 |
| expire_time | DATETIME | NOT NULL | 过期时间 |
| used | TINYINT | DEFAULT 0 | 0 未使用 / 1 已使用 |
| create_time | DATETIME | DEFAULT NOW, INDEX | 创建时间 |

---

## 表 4-16 community_post 社区互助帖

承载邻里互助板块的帖子内容，覆盖求购、赠送、换物三种类型，含浏览量与回复数统计。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| user_id | INT | NOT NULL, INDEX | 发帖人 ID |
| type | TINYINT | NOT NULL, INDEX | 1 求购 / 2 赠送 / 3 换物 |
| title | VARCHAR(100) | NOT NULL | 标题 |
| description | TEXT |  | 详细描述 |
| images | TEXT |  | 图片 URL JSON 数组 |
| want_item | VARCHAR(200) |  | 换物时想换的物品 |
| location | VARCHAR(100) |  | 所在位置 / 小区 / 楼栋 |
| contact | VARCHAR(100) |  | 联系方式（可选） |
| status | TINYINT | DEFAULT 1, INDEX | 1 进行中 / 2 已完成 / 3 已关闭 |
| view_count | INT | DEFAULT 0 | 浏览量 |
| reply_count | INT | DEFAULT 0 | 回复数 |
| create_time | DATETIME | DEFAULT NOW, INDEX | 发布时间 |
| update_time | DATETIME | ON UPDATE | 更新时间 |

---

## 表 4-17 community_post_reply 社区帖回复

存储社区互助帖下的用户回复内容，按 post_id 聚合展示。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| post_id | INT | NOT NULL, INDEX | 帖子 ID |
| user_id | INT | NOT NULL, INDEX | 回复人 ID |
| content | VARCHAR(500) | NOT NULL | 回复内容 |
| create_time | DATETIME | DEFAULT NOW | 回复时间 |

---

## 表 4-18 pickup_point 社区自提点

维护贡江社区内可供线下当面交易的安全自提点列表，含地址、开放时间与说明，下单时可被订单引用。

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INT | PK, AUTO_INCREMENT | 主键 |
| name | VARCHAR(100) | NOT NULL | 地点名称 |
| address | VARCHAR(255) | NOT NULL | 详细地址 |
| description | VARCHAR(500) |  | 说明（如：物业旁、摄像头覆盖） |
| hours | VARCHAR(100) |  | 开放时间 |
| sort | INT | DEFAULT 0 | 排序 |
| status | TINYINT | DEFAULT 1 | 1 启用 / 0 停用 |
| create_time | DATETIME | DEFAULT NOW | 创建时间 |

