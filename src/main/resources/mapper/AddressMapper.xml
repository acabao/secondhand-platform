<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.ylw.mapper.AddressMapper">

    <resultMap id="BaseResultMap" type="com.springboot.ylw.entity.Address">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="receiver" property="receiver"/>
        <result column="phone" property="phone"/>
        <result column="detail" property="detail"/>
        <result column="is_default" property="isDefault"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM address WHERE user_id = #{userId} ORDER BY is_default DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM address WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO address (user_id, receiver, phone, detail, is_default)
        VALUES (#{userId}, #{receiver}, #{phone}, #{detail}, #{isDefault})
    </insert>

    <update id="update">
        UPDATE address
        <set>
            <if test="receiver != null">receiver = #{receiver},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM address WHERE id = #{id}
    </delete>

    <update id="clearDefault">
        UPDATE address SET is_default = 0 WHERE user_id = #{userId}
    </update>

</mapper>
